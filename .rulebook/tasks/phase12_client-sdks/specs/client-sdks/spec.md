# Spec delta: client-sdks

No system spec governs this ecosystem task yet. Scope is defined by `proposal.md`; re-validate at pickup. If the work produces durable contracts (SDK API surface, GUI/back-end coupling, deployment invariants), promote them to a numbered spec in `docs/specs/` before archiving.
